This program is an HTTP server that utilizes the zxcvbn module to rate passwords strength as well as
providing information  relating to the passwords' strength, crackability, suggestions for improving
its security, etc.

This program expects at least one field in JSON data with a "password" field containing the password and
returns a dictionary.
